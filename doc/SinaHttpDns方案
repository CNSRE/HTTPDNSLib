
Sina HTTPDNS 方案


项目解决的问题：

1.DNS劫持。
	
	3月20日15点会议中httpdns讨论会议中刘明生主要提倒两点 域名劫持，和七层篡改/劫持（这个不是httpdns能解决的）。
	基调从nslookup的结果看ww1.sinaimg.cn的第一次解析解析结果是 weiboimg.gslb.sinaedge.com. 该记录的TTL为26188秒。 
	正常情况该解析结果的TTL应该是<=60秒。 我们认为山西移动对ww1.sinaimg.cn域名进行了劫持, 修改了域名解析结果的有效时间和指向。

2.HTTP请求加速

